# API-Basis (optional). Wenn leer/fehlend: Requests gegen /api… (gleicher Origin wie die App).
# - npm run dev / npm run preview: Vite-Proxy (vite.config.ts) → Backend, kein CORS.
# - statisches Hosting ohne Proxy: VITE_API_URL=https://deine-api.example.com setzen.
# VITE_API_URL=http://localhost:8090
#
# Proxy-Ziel für `dev` und `preview`, wenn /api weitergeleitet wird:
# VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8090

# Notion: kein Secret in .env — im Browser unter Einstellungen → Bereich „Verbindungen“ den Internal-Integration-Token (secret_…) eintragen.

# Playwright (`npm run test:e2e`): Backend muss laufen; gleiche DB wie DATABASE_URL_TEST in cos-agent/.env
# SERVICE_TOKEN für global-setup Health-Check (optional)
# E2E_ADMIN_EMAIL=e2e-admin@test.local
# E2E_USER_EMAIL=e2e-user@test.local
# Muss zum Hash in playwright_seed passen (Default wenn unset: Playwright-E2E-2026!)
# E2E_USER_PASSWORD=Playwright-E2E-2026!
